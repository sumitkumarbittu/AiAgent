# Core Dependencies
Flask==2.3.3
Flask-Cors==4.0.0
Werkzeug==2.3.7
python-dotenv==1.0.0
gunicorn==20.1.0
gevent==22.10.2  # Pinned to a version known to work with Python 3.9

# AI & Machine Learning
google-generativeai==0.3.2
google-api-python-client==2.104.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.1.0

# Data Processing & Analysis
pandas==1.5.3  # Pinned to a version known to work with Python 3.9
numpy==1.24.3  # Pinned to a version known to work with Python 3.9

# Dashboard & Visualization
plotly==5.13.1  # Pinned to a version known to work with Python 3.9
dash==2.9.3  # Pinned to a version known to work with Python 3.9
dash-bootstrap-components==1.4.1  # Pinned to a version known to work with Python 3.9

# Web & API
requests==2.31.0

# Required for deployment
setuptools==67.8.0
wheel==0.40.0
python-dateutil==2.8.2

# Alerting & Notifications
yagmail==0.15.293
slack-sdk==3.21.3
flake8==6.1.0

# Utilities
python-json-logger==2.0.7
